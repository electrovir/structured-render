import {defineBookPage} from 'element-book';

export const cardsBookPage = defineBookPage({
    title: 'Cards',
    parent: undefined,
    descriptionParagraphs: [
        'Multiple full cards of structured render data.',
    ],
});
